def get_default_paths() -> dict[str, str]:
    return {
        "cc": "clang",
        "ld": "clang",
        "mlir-opt": "mlir-opt",
        "mlir-translate": "mlir-translate",
        "snax-opt": "snax-opt",
    }
